#include "includes/controlsData.hpp"
// namespace ph = std::placeholders;

// procesData prData{N};

void controlsData::controls(std::string data)
{
    // std::string data;
    std::cout << "Параметр: " << N << "  data: " << data << std::endl;

    // FIXME Comment out object procesData
    // procesData prData(N);

    // FIXME Commented out the cycle
    //  while (request)
    //  {
    //      std::cin >> data;

    // if (std::cin.eof())
    // {
    //     data = "null";
    //     std::cin.clear();
    // };

    // prData.sortData(data);
    //  if (data.compare("stop") == 0)
    //  {
    //      request = false;
    //  }
    //  };
}